<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spring.realworld.infra.mybatis.readservice.UserReadService">
    <select id="findById" resultType="com.spring.realworld.application.data.UserData">
        select * from users where id = #{id}
    </select>

    <select id="findByUsername" resultType="com.spring.realworld.application.data.UserData">
        select * from users where username = #{username}
    </select>
</mapper>